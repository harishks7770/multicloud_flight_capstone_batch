apache-airflow>=2.8,<3.0
apache-airflow-providers-amazon>=8.0
apache-airflow-providers-databricks>=6.0
apache-airflow-providers-snowflake>=5.0
requests>=2.31
boto3>=1.34

# pyspark / delta-spark are intentionally not listed here: the notebook in
# databricks/ runs on a Databricks cluster, which already provides Spark and
# Delta Lake. Installing pyspark locally is only needed if you want to unit
# test the transformation logic outside of Databricks.
